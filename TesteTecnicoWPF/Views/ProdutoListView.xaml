<Window x:Class="TesteTecnicoWPF.Views.ProdutoListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TesteTecnicoWPF.Views"
        mc:Ignorable="d"
        Title="Gerenciamento de Produtos" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Novo Produto" Command="{Binding NovoCommand}" Margin="0,0,5,0"/>
            <Button Content="Editar Selecionado" Command="{Binding EditarCommand}" Margin="0,0,5,0"/>
            <Button Content="Excluir Selecionado" Command="{Binding ExcluirCommand}"/>
        </StackPanel>

        <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,5">
            <StackPanel Orientation="Horizontal" Margin="0,0,10,5">
                <TextBlock Text="Filtrar por Nome:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,10,5">
                <TextBlock Text="Filtrar por Código:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,10,5">
                <TextBlock Text="Valor entre:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding FiltroValorInicial, UpdateSourceTrigger=PropertyChanged, StringFormat=C2}" Width="80"/>
                <TextBlock Text="e" VerticalAlignment="Center" Margin="5,0,5,0"/>
                <TextBox Text="{Binding FiltroValorFinal, UpdateSourceTrigger=PropertyChanged, StringFormat=C2}" Width="80"/>
            </StackPanel>
        </WrapPanel>

        <DataGrid ItemsSource="{Binding ProdutosView}"
                  SelectedItem="{Binding ProdutoSelecionado, Mode=TwoWay}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="Auto"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>